<div class="container">
<h4 class="text-center pl-5 pr-5 m-4 ml-5">◆ 注文履歴一覧 ◆</h4>
<div class='row'>
<div class="col-12 px-sm-2 offset-sm-0">
<table class="table table-bordered">

    <tr class="table-warning">
      <td class="border border-dark text-left"><strong>注文日</strong></td>
      <td class="border border-dark text-left"><strong>配送先</strong></td>
      <td class="border border-dark text-left"><strong>注文商品</strong></td>
      <td class="border border-dark text-left"><strong>支払金額</strong></td>
      <td class="border border-dark text-left"><strong>ステータス</strong></td>
      <td class="border border-dark text-left"><strong>注文詳細</strong></td>
    </tr>

    <% @orders.each do |order| %>
    <tr class="text-center">
      <td class="border border-dark"><%= order.created_at.strftime('%Y/%m/%d') %></td>
      <td class="border border-dark"><%= order.address %></td>
      <td class="border border-dark"><%  order.order_items.each do |order_item| %>
      <%= order_item.item.name %>
          <% end %>
      </td>
      <td class="border border-dark"><%= order.total_payment %></td>
      <td class="border border-dark"><%= order.status_i18n %></td>
      <td class="border border-dark"><%= link_to "表示する", order_path(order.id), class: "btn btn-primary" %></td>
    </tr>
  <% end %>
</table>
</div>
</div>
</div>